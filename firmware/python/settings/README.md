# settings/

| File      | Role                                                         |
|-----------|--------------------------------------------------------------|
| `grid.py` | `CompositionGrid` — rule-of-thirds and Euclid overlays on preview |
| `normal.py` | Standard scene crop helpers                                |

Grid modes: `OFF`, `3x3`, `Euclid`. Selected via Settings → Grid, stored in `config["grid_mode"]`.
